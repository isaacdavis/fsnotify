pipeline {
    agent any
    environment {
    	SRC_DIR = "${env.WORKSPACE}"
    }
    stages {
        stage('Build') {
            steps {
            	sh 'echo ALL IS WELL'
            }
        }
    }
}